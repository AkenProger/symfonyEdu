<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Conferences</title>
</head>
<body>
<div style="display: flex; ">
    {% for key in keys%}
        <div style="padding: 10px; margin: 5px; background: #2d2d2d; color: white; border-radius: 12px; width: 300px; height: 300px">
            <h2>{{ key.id}}</h2>
            <h1>{{ key.name }}</h1>
            <img src="{{ key.img}}" width="250" height="250">
        </div>
    {%  endfor %}
</div>

</body>
</html>